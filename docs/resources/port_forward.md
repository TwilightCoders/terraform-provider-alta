---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "alta_port_forward Resource - alta"
subcategory: ""
description: |-
  One port forward — a destination NAT rule — written through the Alta cloud so the portal shows it.
  Forwards that exist in the portal and not in Terraform are left alone: this resource owns its own rule and nothing else. The change is applied as one gated, commit-confirmed transaction — a single push to the router, rolled back automatically unless the health probes pass.
---

# alta_port_forward (Resource)

One port forward — a destination NAT rule — written through the Alta cloud so the portal shows it.

Forwards that exist in the portal and not in Terraform are left alone: this resource owns its own rule and nothing else. The change is applied as one gated, commit-confirmed transaction — a single push to the router, rolled back automatically unless the health probes pass.

## Example Usage

```terraform
resource "alta_port_forward" "game" {
  site_id   = "aBcDeFgHiJkLmNoPqRsTu"
  device_id = "0a1b2c3d4e5f"

  description = "Game server"
  protocols   = ["udp"]
  ip_version  = "ipv4"
  zone_in     = "wan"
  zone_out    = "lan"
  destination = { port = "8100" }
  translation = { address = "192.0.2.10", port = "8100" }
}

# Only reachable from the office, and on a range of ports.
resource "alta_port_forward" "office_rdp" {
  site_id   = "aBcDeFgHiJkLmNoPqRsTu"
  device_id = "0a1b2c3d4e5f"

  description = "Office desktops"
  protocols   = ["tcp"]
  zone_in     = "wan"
  zone_out    = "lan"
  source      = { address = "203.0.113.0/24" }
  destination = { port = "33890-33899" }
  translation = { address = "192.0.2.20", port = "3389" }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `device_id` (String) Router device id: its MAC address, lowercase without separators.
- `protocols` (Set of String) Protocols to forward.
- `site_id` (String) Alta site id.
- `zone_in` (String) Zone the traffic arrives from, usually `wan`.

### Optional

- `description` (String) Name shown in the portal.
- `destination` (Attributes) Address and port the traffic is sent to. (see [below for nested schema](#nestedatt--destination))
- `id` (String) Alta rule id. Generated the way the portal generates one when not set.
- `ip_version` (String) `ipv4`, `ipv6` or `any`.
- `source` (Attributes) Restrict the forward to these sources. (see [below for nested schema](#nestedatt--source))
- `translation` (Attributes) Internal address and port it is forwarded to. (see [below for nested schema](#nestedatt--translation))
- `zone_out` (String) Zone of the destination host.

<a id="nestedatt--destination"></a>
### Nested Schema for `destination`

Optional:

- `address` (String) Address or CIDR.
- `port` (String) Port or range, e.g. `443` or `50000-50100`.


<a id="nestedatt--source"></a>
### Nested Schema for `source`

Optional:

- `address` (String) Address or CIDR.
- `port` (String) Port or range, e.g. `443` or `50000-50100`.


<a id="nestedatt--translation"></a>
### Nested Schema for `translation`

Optional:

- `address` (String) Address or CIDR.
- `port` (String) Port or range, e.g. `443` or `50000-50100`.
